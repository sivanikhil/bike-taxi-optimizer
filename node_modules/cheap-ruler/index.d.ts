/**
 * A collection of very fast approximations to common geodesic measurements. Useful for performance-sensitive code that measures things on a city scale.
 */
export default class CheapRuler {
    /**
     * Creates a ruler object from tile coordinates (y and z).
     *
     * @param {number} y
     * @param {number} z
     * @param {keyof typeof factors} [units='kilometers']
     * @returns {CheapRuler}
     * @example
     * const ruler = cheapRuler.fromTile(1567, 12);
     * //=ruler
     */
    static fromTile(y: number, z: number, units?: keyof typeof factors): CheapRuler;
    /**
     * Multipliers for converting between units.
     *
     * @example
     * // convert 50 meters to yards
     * 50 * CheapRuler.units.yards / CheapRuler.units.meters;
     */
    static get units(): {
        kilometers: number;
        miles: number;
        nauticalmiles: number;
        meters: number;
        metres: number;
        yards: number;
        feet: number;
        inches: number;
    };
    /**
     * Creates a ruler instance for very fast approximations to common geodesic measurements around a certain latitude.
     *
     * @param {number} lat latitude
     * @param {keyof typeof factors} [units='kilometers'] one of: kilometers, miles, nauticalmiles, meters, metres, yards, feet, inches
     * @example
     * const ruler = cheapRuler(35.05, 'miles');
     * //=ruler
     */
    constructor(lat: number, units?: keyof typeof factors);
    kx: number;
    ky: number;
    /**
     * Given two points of the form [longitude, latitude], returns the distance.
     *
     * @param {readonly [number, number]} a point [longitude, latitude]
     * @param {readonly [number, number]} b point [longitude, latitude]
     * @returns {number} distance
     * @example
     * const distance = ruler.distance([30.5, 50.5], [30.51, 50.49]);
     * //=distance
     */
    distance(a: readonly [number, number], b: readonly [number, number]): number;
    /**
     * Returns the bearing between two points in angles.
     *
     * @param {readonly [number, number]} a point [longitude, latitude]
     * @param {readonly [number, number]} b point [longitude, latitude]
     * @returns {number} bearing
     * @example
     * const bearing = ruler.bearing([30.5, 50.5], [30.51, 50.49]);
     * //=bearing
     */
    bearing(a: readonly [number, number], b: readonly [number, number]): number;
    /**
     * Returns a new point given distance and bearing from the starting point.
     *
     * @param {readonly [number, number]} p point [longitude, latitude]
     * @param {number} dist distance
     * @param {number} bearing
     * @returns {[number, number]} point [longitude, latitude]
     * @example
     * const point = ruler.destination([30.5, 50.5], 0.1, 90);
     * //=point
     */
    destination(p: readonly [number, number], dist: number, bearing: number): [number, number];
    /**
     * Returns a new point given easting and northing offsets (in ruler units) from the starting point.
     *
     * @param {readonly [number, number]} p point [longitude, latitude]
     * @param {number} dx easting
     * @param {number} dy northing
     * @returns {[number, number]} point [longitude, latitude]
     * @example
     * const point = ruler.offset([30.5, 50.5], 10, 10);
     * //=point
     */
    offset(p: readonly [number, number], dx: number, dy: number): [number, number];
    /**
     * Given a line (an array of points), returns the total line distance.
     *
     * @param {readonly [number, number][]} points [longitude, latitude]
     * @returns {number} total line distance
     * @example
     * const length = ruler.lineDistance([
     *     [-67.031, 50.458], [-67.031, 50.534],
     *     [-66.929, 50.534], [-66.929, 50.458]
     * ]);
     * //=length
     */
    lineDistance(points: readonly [number, number][]): number;
    /**
     * Given a polygon (an array of rings, where each ring is an array of points), returns the area.
     *
     * @param {readonly [number, number][][]} polygon
     * @returns {number} area value in the specified units (square kilometers by default)
     * @example
     * const area = ruler.area([[
     *     [-67.031, 50.458], [-67.031, 50.534], [-66.929, 50.534],
     *     [-66.929, 50.458], [-67.031, 50.458]
     * ]]);
     * //=area
     */
    area(polygon: readonly [number, number][][]): number;
    /**
     * Returns the point at a specified distance along the line.
     *
     * @param {readonly [number, number][]} line
     * @param {number} dist distance
     * @returns {[number, number]} point [longitude, latitude]
     * @example
     * const point = ruler.along(line, 2.5);
     * //=point
     */
    along(line: readonly [number, number][], dist: number): [number, number];
    /**
     * Returns the distance from a point `p` to a line segment `a` to `b`.
     *
     * @param {readonly [number, number]} p point [longitude, latitude]
     * @param {readonly [number, number]} a segment point 1 [longitude, latitude]
     * @param {readonly [number, number]} b segment point 2 [longitude, latitude]
     * @returns {number} distance
     * @example
     * const distance = ruler.pointToSegmentDistance([-67.04, 50.5], [-67.05, 50.57], [-67.03, 50.54]);
     * //=distance
     */
    pointToSegmentDistance(p: readonly [number, number], a: readonly [number, number], b: readonly [number, number]): number;
    /**
     * Returns an object of the form {point, index, t, dist}, where point is closest point on the line
     * from the given point, index is the start index of the segment with the closest point,
     * t is a parameter from 0 to 1 that indicates where the closest point is on that segment,
     * and dist is the distance from the point to the line.
     *
     * @param {readonly [number, number][]} line
     * @param {readonly [number, number]} p point [longitude, latitude]
     * @returns {{point: [number, number], index: number, t: number, dist: number}} {point, index, t, dist}
     * @example
     * const point = ruler.pointOnLine(line, [-67.04, 50.5]).point;
     * //=point
     */
    pointOnLine(line: readonly [number, number][], p: readonly [number, number]): {
        point: [number, number];
        index: number;
        t: number;
        dist: number;
    };
    /**
     * Returns a part of the given line between the start and the stop points (or their closest points on the line).
     *
     * @param {readonly [number, number]} start point [longitude, latitude]
     * @param {readonly [number, number]} stop point [longitude, latitude]
     * @param {readonly [number, number][]} line
     * @returns {[number, number][]} line part of a line
     * @example
     * const line2 = ruler.lineSlice([-67.04, 50.5], [-67.05, 50.56], line1);
     * //=line2
     */
    lineSlice(start: readonly [number, number], stop: readonly [number, number], line: readonly [number, number][]): [number, number][];
    /**
     * Returns a part of the given line between the start and the stop points indicated by distance along the line.
     * Returns an empty array if start is beyond the total line length.
     *
     * @param {number} start start distance
     * @param {number} stop stop distance
     * @param {readonly [number, number][]} line
     * @returns {[number, number][]} part of a line
     * @example
     * const line2 = ruler.lineSliceAlong(10, 20, line1);
     * //=line2
     */
    lineSliceAlong(start: number, stop: number, line: readonly [number, number][]): [number, number][];
    /**
     * Given a point, returns a bounding box object ([w, s, e, n]) created from the given point buffered by a given distance.
     *
     * @param {readonly [number, number]} p point [longitude, latitude]
     * @param {number} buffer
     * @returns {[number, number, number, number]} bbox ([w, s, e, n])
     * @example
     * const bbox = ruler.bufferPoint([30.5, 50.5], 0.01);
     * //=bbox
     */
    bufferPoint(p: readonly [number, number], buffer: number): [number, number, number, number];
    /**
     * Given a bounding box, returns the box buffered by a given distance.
     *
     * @param {readonly [number, number, number, number]} bbox ([w, s, e, n])
     * @param {number} buffer
     * @returns {[number, number, number, number]} bbox ([w, s, e, n])
     * @example
     * const bbox = ruler.bufferBBox([30.5, 50.5, 31, 51], 0.2);
     * //=bbox
     */
    bufferBBox(bbox: readonly [number, number, number, number], buffer: number): [number, number, number, number];
    /**
     * Returns true if the given point is inside in the given bounding box, otherwise false.
     *
     * @param {readonly [number, number]} p point [longitude, latitude]
     * @param {readonly [number, number, number, number]} bbox ([w, s, e, n])
     * @returns {boolean}
     * @example
     * const inside = ruler.insideBBox([30.5, 50.5], [30, 50, 31, 51]);
     * //=inside
     */
    insideBBox(p: readonly [number, number], bbox: readonly [number, number, number, number]): boolean;
}
declare namespace factors {
    let kilometers: number;
    let miles: number;
    let nauticalmiles: number;
    let meters: number;
    let metres: number;
    let yards: number;
    let feet: number;
    let inches: number;
}
export {};
